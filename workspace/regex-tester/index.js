const { testPattern, validateEmail, validatePhone, validateUrl, replaceText } = require('./tester.js');

module.exports = {
  testPattern,
  validateEmail,
  validatePhone,
  validateUrl,
  replaceText
};
