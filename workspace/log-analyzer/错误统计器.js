# 错误统计器

// 统计错误数量
const countErrors = (logs) => {
  // 实现错误数量统计逻辑
};

// 按类型分组
const groupByType = (logs) => {
  // 实现按类型分组逻辑
};

// 显示最频繁的错误
const showMostFrequent = (logs) => {
  // 实现显示最频繁的错误逻辑
};

module.exports = {
  countErrors,
  groupByType,
  showMostFrequent
};